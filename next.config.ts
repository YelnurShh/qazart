/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  images: {
    unoptimized: true, // 🟡 ⬅️ Міне осы жолды қосу керек
  },
};

module.exports = nextConfig;
